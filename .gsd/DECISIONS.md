# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-zku9aj | library | GitHub API testing strategy | vercel-labs/emulate started as Go exec.Command subprocess | Production-fidelity GitHub API emulation without mocked HTTP. Real API responses, stateful repos/releases. User specifically requested emulate. | No | collaborative |
| D002 | M001-zku9aj | arch | FQN type location | In the tool package alongside Package and ResolvedTool | FQN types are identity types that Package will eventually carry. tool package is tiny (62 lines) and the natural home for static identity types. | No | collaborative |
| D003 | M001-zku9aj | arch | When to define the PackageSource abstraction | PackageSource interface defined in S05 alongside first implementation (GitHubReleaseSource) | Defining the interface alongside its first real implementation ensures the contract is grounded. S06 implements the same interface. S08 composes both trivially because the contract is already proven. | No | agent |
| D004 | M001-zku9aj | scope | Toolset file scope for M001 | Minimal: packages map + tools list only. No bindings, credentials, context, resource_bindings. | Bindings and credentials are orthogonal to registry resolution. Including them would bloat the milestone without proving the registry works. Deferred to R023. | Yes — when bindings milestone starts | collaborative |
| D005 | M001-zku9aj | convention | Design authority for registry implementation | docs/rfc-tool-registry.md is the authoritative design document. All context files and research artifacts must reference it. | RFC contains all design decisions, open questions, alternative analysis, and detailed specs. Prevents agents from reinventing answers already decided in the RFC. | No | collaborative |
| D006 | M001-zku9aj/S01/T01 | test-infrastructure | How emulate-based test fixtures create GitHub repositories | Use POST /user/repos and treat repository ownership as the auto-created admin user in emulate v0.3.0 instead of relying on org creation endpoints. | A live probe against emulate v0.3.0 showed /admin/orgs and /orgs/:owner/repos returning 404, while /user/repos succeeds and returns full_name admin/<repo>. Aligning the seed helper with that real behavior keeps the lifecycle tests stable and gives downstream registry slices a consistent owner model. | Yes | agent |
| D007 | M001-zku9aj/S01/T03 | workflow/testing | GitHub testing branch policy | Do not push to `main` when testing GitHub-related workflow or integration changes. Use local verification, emulate-backed tests, CI/workflow linting, and read-only inspection by default; if remote execution is needed later, use a non-`main` branch only with explicit user confirmation. | The user overrode the previous working assumption during S01/T03. The milestone already proves GitHub behavior through emulate and local/CI checks, so pushing to the protected mainline adds risk without adding required evidence. This supersedes any implicit plan assumption that GitHub testing could use `main` as a verification path. | No | human |
| D008 |  | requirement | R001 | validated | Table-driven tests in tool/fqn_test.go prove parse/format round-trips for all FQN types, semver and pseudo-version acceptance, and rejection of invalid inputs. | Yes | agent |
| D009 |  | architecture | ErrReleaseNotFound sentinel error for PackageSource | Exposed ErrReleaseNotFound as a package-level sentinel so callers can distinguish missing releases from other failures | S08 resolver orchestration needs to detect "not found" to fall back to git-source (S06). A sentinel error is the idiomatic Go pattern and avoids string matching. | Yes | agent |
| D010 | M001-zku9aj/S07 | arch | How GitSourceFallback validates pseudo-version commits | For pseudo versions, GitSourceFallback must full-clone, resolve the 12-character commit prefix to a full commit, and treat a mismatch between the pseudo-version timestamp and the commit author timestamp as a hard error before packaging. | The pseudo-version format is already parsed in tool.Version, and enforcing the timestamp match keeps pins meaningful, catches malformed-but-syntactically-valid versions early, and preserves the existing tagged fast path without widening PackageSource. | Yes — if interoperability evidence shows timestamp enforcement is too strict | agent |
| D011 | M001-zku9aj S07 T02 | architecture | How GitSourceFallback should fetch tagged versus pseudo-version git references | Keep the existing shallow clone-by-tag path for tagged versions, and add a separate pseudo-version path that performs a full clone, rev-parse commit resolution, author-timestamp validation, and commit checkout before packaging. | Tagged requests already have an efficient fast path, while pseudo-versions need full history access to resolve commit prefixes and validate the encoded timestamp before packaging. Splitting the paths preserves existing performance for tags and makes pseudo-version failure phases observable in tests. | Yes | agent |
