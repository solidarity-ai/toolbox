Call new_super_tool_session first. It returns a tb_session and the notebook instructions. Reuse that tb_session on every super_tool call.
Reuse the same tb_session on await_super_tool_approvals calls.
Packages:
- issues