export declare const tools: {
  calc: {
    add(a: number, b: number): number; // readonly
    asyncAdd(a: number, b: number): number; // readonly
    sub(a: number, b: number): number; // readonly
  };
};
