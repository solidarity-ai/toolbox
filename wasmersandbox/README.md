# wasmersandbox

`wasmersandbox` is the Rust runtime for the typescript+wasmer-sandbox path.

For now this folder is only a scaffold. The Go-side seam is exercised through
`runtime/tswasmer`, and real sandbox internals can replace the stub later.
