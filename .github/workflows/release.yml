name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-binaries:
    name: Build ${{ matrix.binary_name }}
    runs-on: ${{ matrix.runner }}
    env:
      GOPRIVATE: github.com/mackross/*
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            binary_name: toolbox-linux-x64
          - runner: ubuntu-24.04-arm
            binary_name: toolbox-linux-arm64
          - runner: macos-13
            binary_name: toolbox-darwin-x64
          - runner: macos-14
            binary_name: toolbox-darwin-arm64
          - runner: windows-2022
            binary_name: toolbox-win32-x64.exe
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"
          cache: true

      - name: Build toolbox
        shell: bash
        working-directory: cmd/toolbox
        run: |
          mkdir -p ../../dist
          go build -o "../../dist/${{ matrix.binary_name }}" .

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary_name }}
          path: dist/${{ matrix.binary_name }}

  publish:
    name: Publish release
    runs-on: ubuntu-latest
    needs: build-binaries
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Download binary artifacts
        uses: actions/download-artifact@v4
        with:
          path: release/raw-binaries
          merge-multiple: true

      - name: Build release assets
        run: node release/scripts/build-release-assets.mjs --version "${GITHUB_REF_NAME}" --binaries-dir release/raw-binaries --out-dir release/assets

      - name: Prepare npm dist
        run: node release/scripts/prepare-npm-dist.mjs --version "${GITHUB_REF_NAME}" --binaries-dir release/raw-binaries --out-dir release/dist/npm

      - name: Publish npm packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: node release/scripts/publish-npm.mjs --dist-dir release/dist/npm

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: release/assets/*
