# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-zku9aj | library | GitHub API testing strategy | vercel-labs/emulate started as Go exec.Command subprocess | Production-fidelity GitHub API emulation without mocked HTTP. Real API responses, stateful repos/releases. User specifically requested emulate. | No | collaborative |
| D002 | M001-zku9aj | arch | FQN type location | In the tool package alongside Package and ResolvedTool | FQN types are identity types that Package will eventually carry. tool package is tiny (62 lines) and the natural home for static identity types. | No | collaborative |
| D003 | M001-zku9aj | arch | When to define the PackageSource abstraction | PackageSource interface defined in S05 alongside first implementation (GitHubReleaseSource) | Defining the interface alongside its first real implementation ensures the contract is grounded. S06 implements the same interface. S08 composes both trivially because the contract is already proven. | No | agent |
| D004 | M001-zku9aj | scope | Toolset file scope for M001 | Minimal: packages map + tools list only. No bindings, credentials, context, resource_bindings. | Bindings and credentials are orthogonal to registry resolution. Including them would bloat the milestone without proving the registry works. Deferred to R023. | Yes — when bindings milestone starts | collaborative |
| D005 | M001-zku9aj | convention | Design authority for registry implementation | docs/rfc-tool-registry.md is the authoritative design document. All context files and research artifacts must reference it. | RFC contains all design decisions, open questions, alternative analysis, and detailed specs. Prevents agents from reinventing answers already decided in the RFC. | No | collaborative |
| D006 | M001-zku9aj/S01/T01 | test-infrastructure | How emulate-based test fixtures create GitHub repositories | Use POST /user/repos and treat repository ownership as the auto-created admin user in emulate v0.3.0 instead of relying on org creation endpoints. | A live probe against emulate v0.3.0 showed /admin/orgs and /orgs/:owner/repos returning 404, while /user/repos succeeds and returns full_name admin/<repo>. Aligning the seed helper with that real behavior keeps the lifecycle tests stable and gives downstream registry slices a consistent owner model. | Yes | agent |
| D007 | M001-zku9aj/S01/T03 | workflow/testing | GitHub testing branch policy | Do not push to `main` when testing GitHub-related workflow or integration changes. Use local verification, emulate-backed tests, CI/workflow linting, and read-only inspection by default; if remote execution is needed later, use a non-`main` branch only with explicit user confirmation. | The user overrode the previous working assumption during S01/T03. The milestone already proves GitHub behavior through emulate and local/CI checks, so pushing to the protected mainline adds risk without adding required evidence. This supersedes any implicit plan assumption that GitHub testing could use `main` as a verification path. | No | human |
| D008 |  | requirement | R001 | validated | Table-driven tests in tool/fqn_test.go prove parse/format round-trips for all FQN types, semver and pseudo-version acceptance, and rejection of invalid inputs. | Yes | agent |
| D009 |  | architecture | ErrReleaseNotFound sentinel error for PackageSource | Exposed ErrReleaseNotFound as a package-level sentinel so callers can distinguish missing releases from other failures | S08 resolver orchestration needs to detect "not found" to fall back to git-source (S06). A sentinel error is the idiomatic Go pattern and avoids string matching. | Yes | agent |
| D010 | M001-zku9aj/S07 | arch | How GitSourceFallback validates pseudo-version commits | For pseudo versions, GitSourceFallback must full-clone, resolve the 12-character commit prefix to a full commit, and treat a mismatch between the pseudo-version timestamp and the commit author timestamp as a hard error before packaging. | The pseudo-version format is already parsed in tool.Version, and enforcing the timestamp match keeps pins meaningful, catches malformed-but-syntactically-valid versions early, and preserves the existing tagged fast path without widening PackageSource. | Yes — if interoperability evidence shows timestamp enforcement is too strict | agent |
| D011 | M001-zku9aj S07 T02 | architecture | How GitSourceFallback should fetch tagged versus pseudo-version git references | Keep the existing shallow clone-by-tag path for tagged versions, and add a separate pseudo-version path that performs a full clone, rev-parse commit resolution, author-timestamp validation, and commit checkout before packaging. | Tagged requests already have an efficient fast path, while pseudo-versions need full history access to resolve commit prefixes and validate the encoded timestamp before packaging. Splitting the paths preserves existing performance for tags and makes pseudo-version failure phases observable in tests. | Yes | agent |
| D012 | M001-zku9aj/S07 | test-infrastructure | Pseudo-version fixture timestamp derivation | Derive pseudo-version timestamps from the committed git author date in UTC | The S07 pseudo-version fixture and resolver both depend on the same timestamp semantics. Using git's recorded author timestamp in UTC keeps fixture output aligned with tool.Version pseudo-version parsing and prevents tests from passing with metadata that does not match the actual commit. | Yes | agent |
| D013 | M001-zku9aj/S07 follow-up | architecture | Pseudo-version timestamp semantics | Use git commit time (`%cI`), not author time (`%aI`), when deriving and validating pseudo-version timestamps | The prior S07 implementation used author time, but Go pseudo-version semantics use commit time. The fixture now intentionally makes author and commit timestamps differ, and both the resolver and tests validate against commit time in UTC. | No | human |
| D014 | M001-zku9aj/S08 | architecture | Resolver always returns via Cache.LoadArchive | Both cache hits and post-fetch writes go through Cache.LoadArchive so the same integrity verification path is exercised regardless of package origin | Ensures that every resolved package passes the same sha256 verification as AddFromArchive, eliminating a class of bugs where fetched-but-unchecked bytes bypass integrity. | No | agent |
| D015 | M001-zku9aj/S08 | architecture | Source chain fallback gated on ErrReleaseNotFound | Only ErrReleaseNotFound triggers fallback to the next source; all other errors short-circuit immediately | Prevents masking real failures (auth errors, network timeouts, corrupt data) behind a fallback that would succeed for the wrong reasons. Makes failure diagnosis deterministic. | No | agent |
| D016 | M001-zku9aj/S08 | requirement | R006 | validated | TestBuilderAddFromRegistry proves: nil-resolver returns ErrNoResolver, invalid inputs return parse errors, pre-populated cache resolves successfully producing correct package name, resolved package appears in Resolve() output identically to AddFromDir/AddFromArchive. | Yes | agent |
| D017 | M001-zku9aj/S09 planning | architecture | S09 toolset file API and reference form | `toolset.Load(filename)` returns a validated `*ToolsetFile`, package resolution happens in `(*ToolsetFile).Resolve(ctx, resolver)`, and `tools[].tool` entries must use full FQNs whose module and version exactly match the `packages` map. | Separating parse-time validation from registry resolution keeps configuration errors distinct from fetch/load failures, preserves the parsed structure for downstream lockfile/replace work, and avoids ambiguous short-name expansion before package manifests are loaded. | Yes — revisit when replace directives, lockfiles, or short-name expansion are implemented. | agent |
| D018 | M001-zku9aj/S09/T01 follow-up | validation | How toolbox.toolset.json should validate shape versus semantics | Use embedded JSON Schema for file shape validation, then run explicit Go semantic validation; allow empty packages/tools at this layer. | Schema provides consistent structural errors for required fields, types, and unexpected properties, while semantic rules like module/version parsing and tool-to-package cross-checks remain clearer in Go. Allowing empty packages and tools keeps the file layer as a permissive declarative container and avoids prematurely encoding tool-selection semantics in M001. | Yes — revisit when tool selection semantics, bindings, or lockfile-driven constraints become first-class. | collaborative |
| D019 |  | requirement | R007 | validated | S09 verification proved that toolset.Load parses and validates toolbox.toolset.json with schema and semantic checks, and TestToolsetFileResolve proved declared packages resolve through Builder.AddFromRegistry using the shared resolver/cache path. Commands run: `GOWORK=$(pwd)/go.work go test ./toolset -v -count=1 -run TestToolsetFileLoad -timeout 30s`, `GOWORK=$(pwd)/go.work go test ./toolset -v -count=1 -run TestToolsetFileResolve -timeout 30s`, and `GOWORK=$(pwd)/go.work go test ./toolset/... -v -count=1 -timeout 30s`. | Yes | agent |
| D020 | M001-zku9aj / S10 planning | architecture | Where lockfile provenance and verification live for declarative toolset resolution | Keep lockfile ownership in `toolset`, add a provenance-bearing resolver result in `registry`, and have declarative lock verification continue through `ToolsetFile.Resolve` -> `Builder.AddFromRegistry` -> `registry.Resolver` rather than adding a second cache/verification path. | R008 needs `archive_sha256`, `git_sha`, `resolved_from`, and `resolved_at` at the declarative toolset boundary, but S09 already established that declarative and imperative assembly must share one builder/resolver/cache path. Keeping file I/O and lock ownership in `toolset` preserves the repo-local lockfile contract, while a richer resolver result lets both cache-hit verification and source provenance flow through the existing registry seam without duplicating integrity logic or silently bypassing fallback behavior. | Yes | agent |
| D021 | M001-zku9aj / S10 planning | validation | How lockfile shape validation should be implemented | Validate `*.toolset.lock` files with embedded JSON Schema for structural shape, then apply Go semantic validation for package-key and provenance rules. | This keeps S10 aligned with S09's proven pattern for config files: schema catches top-level structure, required fields, and type errors early, while Go code handles cross-field and semantic rules such as `<module>@<version>` keys and provenance semantics. It improves error locality without duplicating semantic logic in schema alone. | Yes | human |
| D022 | M001-zku9aj S10 T01 | architecture | How toolset lockfiles are validated and written | Validate toolset lockfiles in two phases (embedded JSON Schema, then Go semantic checks) and write them with sorted package-key JSON via temp-file rename | Schema validation catches structural shape errors before any semantic trust is placed in entries, while semantic validation enforces package-key, hash, provenance, and timestamp rules with entry-local context. Stable sorted encoding makes lockfile diffs deterministic, and temp-file rename avoids partial rewrites when later resolve code updates sibling lockfiles. | Yes | agent |
| D023 | M001-zku9aj/S10/T03 | architecture | Expose lock-aware registry resolution to declarative toolsets without forking the builder path | Added Builder.AddFromRegistryWithExpected to return trusted resolver metadata while keeping AddFromRegistry as the compatibility wrapper. | ToolsetFile.Resolve needs lock verification metadata for cache hits and refetches, but imperative callers should continue using the existing builder API. Returning metadata through a narrow builder extension preserves one shared resolution path and keeps stop-on-first-error behavior anchored in Builder/Resolver composition. | Yes | agent |
